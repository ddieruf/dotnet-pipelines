---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: ubuntu #microsoft/dotnet
    tag: 16.04 #latest

params:
  ARTIFACTORY_REPO_ID:
  ARTIFACTORY_TOKEN:
  ARTIFACTORY_HOST:
  SMOKE_TEST_ARTIFACT_NAME:
  SMOKE_TEST_DLL_NAME:
  DOTNET_VERSION:
  DOTNET_FRAMEWORK:
  DOTNET_PLATFORM:
  DOTNET_TEST_LOGGER:

inputs:
  - name: task-scripts
  - name: concourse-tasks
  - name: keyval

outputs:
  - name: keyvalout

run:
  path: concourse-tasks/smoke-test/task.sh
