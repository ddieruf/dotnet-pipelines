---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: ubuntu #pivotalservices/docker-concourse-cf-tools
    tag: 16.04 #latest

params:
  PAAS_STAGE_ORG:
  PAAS_STAGE_PASSWORD:
  PAAS_STAGE_SPACE:
  PAAS_STAGE_USERNAME:
  PAAS_STAGE_API_URL:
  PAAS_STAGE_APP_NAME:
  ARTIFACTORY_REPO_ID:
  ARTIFACTORY_TOKEN:
  ARTIFACTORY_HOST:

inputs:
  - name: task-scripts
  - name: concourse-tasks
  - name: keyval

outputs:
  - name: keyvalout

run:
  path: concourse-tasks/push-to-cf-stage/task.sh
