---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ubuntu
    tag: 16.04

inputs:
  - name: task-scripts
  - name: concourse-tasks

outputs:
  - name: keyvalout

run:
  path: concourse-tasks/initialize-pipeline/task.sh