---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ubuntu
    tag: xenial

params:
  GIT_EMAIL:
  GIT_NAME:

inputs:
  - name: task-scripts
  - name: concourse-tasks
  - name: src-version
  - name: keyval

outputs:
  - name: updated-version
  - name: keyvalout

run:
  path: concourse-tasks/generate-version/task.sh