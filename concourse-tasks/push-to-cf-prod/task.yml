---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: ubuntu #pivotalservices/docker-concourse-cf-tools
    tag: 16.04 #latest

params:
  PAAS_PROD_ORG:
  PAAS_PROD_PASSWORD:
  PAAS_PROD_SPACE:
  PAAS_PROD_USERNAME:
  PAAS_PROD_API_URL:
  PAAS_PROD_APP_NAME:
  ARTIFACTORY_REPO_ID:
  ARTIFACTORY_TOKEN:
  ARTIFACTORY_HOST:

inputs:
  - name: task-scripts
  - name: concourse-tasks
  - name: keyval

outputs:
  - name: keyvalout

run:
  path: concourse-tasks/push-to-cf-prod/task.sh
